services:
  app:
    build:
      context: ..
      dockerfile: build/Dockerfile
    environment:
      - USE_DB=true
      - DB_HOST=db
      - DB_USER=user
      - DB_PASSWORD=password
      - DB_NAME=services_db
      - DB_PORT=5432
    ports:
      - "8080:8080"
    depends_on:
      - db
