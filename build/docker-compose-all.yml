services:
  app:
    build:
      context: ..
      dockerfile: build/Dockerfile
    environment:
      - USE_DB=false
    ports:
      - "8080:8080"

  ui:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    depends_on:
      - app
